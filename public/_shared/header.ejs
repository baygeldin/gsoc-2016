<header class="post-header">
    <a class="blog-logo" href="<%= blog.url %>">
        <% if(blog.logo) { %>
            <img src="<%= blog.url %><%= blog.logo %>" alt="Blog Logo" />
        <% } else { %>
            <span class="blog-title"><%= blog.title %></span>
        <% } %>
    </a>
</header>
